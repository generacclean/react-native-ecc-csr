# Dependencies come from the public registry. Publishing goes to GitHub Packages through
# publishConfig in package.json (see PUBLISHING.md); the old CodeArtifact registry is retired.
registry=https://registry.npmjs.org/
